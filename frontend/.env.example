# Copia a .env.local para anular. En CI/hosting suele definirse en el panel.
# Producción: debe ser el dominio API actual (nunca el Render antiguo).
VITE_API_URL=https://api.xn--colormetra-s8a.com

# ── Feature flags premium ─────────────────────────────────────────────
# Hair Studio AI generativo (off por default = simulación local).
# VITE_ENABLE_GENERATIVE_HAIR_EDIT=false
#
# Virtual Try-On / módulo de maniquí. Si es false, se muestra solo el
# bloque elegante "Coming Soon" sobre una previsualización bloqueada.
# VITE_ENABLE_VIRTUAL_TRYON=false
